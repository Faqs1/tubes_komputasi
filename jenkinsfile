pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                git credentialsId: 'b6dc3518-4b6e-4774-a292-d5aeda03de68',git 'https://github.com/tubes_komputasi.git'
                sh 'mvn clean install' 
            }
        }

        stage('Deploy') {
            steps {
                script {
                    docker.build('tubes').push('tubes')
                }
            }
        }
    }

    post {
        always {
            archiveArtifacts artifacts: '**/target/*.jar', allowEmptyArchive: true
            junit 'target/surefire-reports/*.xml'
        }
    }
}

