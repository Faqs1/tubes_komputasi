pipeline {
    agent any
    stages {
        stage('Clone Repository') {
            steps {
                git branch:'main', url:  'https://github.com/Faqs1/tubes_komputasi.git'
            }
        }
     
        stage('Run Ansible Playbook') {
            steps {
                 bat 'mariadb.yml -i hosts'
            }
        }
    }
    post {
        success {
            echo 'Deployment successful!'
        }
        failure {
            echo 'Deployment failed!'
        }
    }
}
